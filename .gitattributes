# Default: normalize line endings to LF in the repo
* text=auto

# Shell scripts must always be LF (Docker containers run on Linux)
*.sh text eol=lf

# Dockerfiles
Dockerfile text eol=lf
*.dockerfile text eol=lf

# Front-end image files are copied verbatim into a Linux image and served from it
clients/*/docker/** text eol=lf
