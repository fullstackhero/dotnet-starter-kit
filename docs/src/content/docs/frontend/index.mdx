---
title: Frontend
lastUpdated: 2026-05-19
description: Two React + Vite + TypeScript apps ship with the kit — an admin console for operators and a tenant dashboard for end users.
sidebar:
  order: 1
pageType: guide
seo:
  title: 'Frontend in .NET 10 — React + Vite admin + dashboard'
  description: 'fullstackhero ships two production React 19 + Vite 7 + TypeScript 5.7 frontends — clients/admin (operator console) and clients/dashboard (tenant-facing). Auth, multitenancy, permissions, and realtime wired against the API out of the box.'
  keywords: 'fullstackhero react frontend, vite admin console, react dashboard .net 10, jwt auth react vite, tenant theme react'
---

Two React + Vite + TypeScript apps ship in `clients/` — both production-grade, both wired against the API's auth + multitenancy + realtime out of the box. Pick the right one for your audience: **admin** is for operators (back-office, support, ops); **dashboard** is for end users (the people who pay you).

<Callout type="tip" title="They share patterns, not code">
Both apps follow the same conventions for auth, API integration, route guards, and realtime. They don't share a component library — the admin's audit log + impersonation pages don't belong in the user-facing dashboard, and the dashboard's chat / catalog pages don't belong in admin. Sharing patterns + diverging on features is the right axis.
</Callout>

## The two apps

<SectionIndex section="frontend" />

## Quick facts

| | **clients/admin** | **clients/dashboard** |
|---|---|---|
| Audience | Operators, support, ops | End users (tenant members) |
| Default port | 5173 | 5174 |
| Pages | Users, Roles, Tenants, Impersonation, Audits, Webhooks, Billing, Health, Notifications, Settings | Catalog, Chat, Files, Tickets, Identity (profile + sessions + 2FA), System, Settings |
| Realtime | SignalR notifications | SignalR (chat + notifications) + SSE feeds |
| Auth | Same JWT bearer + refresh + 2FA flow as the API | Same |
| Theming | Static (admin chrome) | Per-tenant theme from the Multitenancy module |

Both run via `npm run dev` (Vite dev server with HMR) and build to static assets via `npm run build`. The kit's Aspire AppHost orchestrates both alongside the API for one-command local dev.

## Related

- [Admin console](/docs/frontend/admin/) — operator-facing app.
- [Tenant dashboard](/docs/frontend/dashboard/) — end-user app.
- [Frontend architecture](/docs/frontend/architecture/) — shared patterns.
- [Theming](/docs/frontend/theming/) — per-tenant brand customisation.
- [Local development](/docs/frontend/development/) — running, building, deploying.
